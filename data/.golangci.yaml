run:
  # Include test files or not.
  # Default: true
  tests: true

linters:
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - contextcheck
    - cyclop
    - deadcode
    - depguard
    - dogsled
    - dupl
    - durationcheck
    - errcheck
    - errname
    - errorlint
    - exportloopref
    - forbidigo
    - forcetypeassert
    - funlen
    - gci
    - gochecknoglobals
    - gochecknoinits
    - gocognit
    - goconst
#    - gocritic
#    - gocyclo
#    - godot
#    - godox
#    - goerr113
#    - gofmt
#    - gofumpt
#    - goheader
#    - goimports
#    - golint
#    - gomnd
#    - gomoddirectives
#    - gomodguard
#    - goprintffuncname
#    - gosec
#    - gosimple
#    - govet
#    - ifshort
#    - importas
#    - ineffassign
#    - interfacer
#    - ireturn
#    - lll
#    - makezero
#    - maligned
#    - misspell
#    - nakedret
#    - nestif
#    - nilerr
#    - nilnil
#    - nlreturn
#    - noctx
#    - nolintlint
#    - paralleltest
#    - prealloc
#    - predeclared
#    - promlinter
#    - revive
#    - rowserrcheck
#    - scopelint
#    - sqlclosecheck
#    - staticcheck
#    - structcheck
#    - stylecheck
#    - tagliatelle
#    - tenv
#    - testpackage
#    - thelper
#    - tparallel
#    - typecheck
#    - unconvert
#    - unparam
#    - unused
#    - varcheck
#    - varnamelen
    - wastedassign
    - whitespace
    - wrapcheck
    - wsl
  # Disable specific linter
  # https://golangci-lint.run/usage/linters/#disabled-by-default-linters--e--enable
  disable:
    - exhaustive

issues:
  # Independently of option `exclude` we use default exclude patterns,
  # it can be disabled by this option.
  # To list all excluded by default patterns execute `golangci-lint run --help`.
  # Default: true.
  exclude-use-default: true
  # List of regexps of issue texts to exclude.
  #
  # But independently of this option we use default exclude patterns,
  # it can be disabled by `exclude-use-default: false`.
  # To list all excluded by default patterns execute `golangci-lint run --help`
  #
  # Default: []
  exclude:
    # exclude-use-default EXC0001 errcheck: Almost all programs ignore errors on these functions and in most cases it's ok
    - Error return value of .((os\.)?std(out|err)\..*|.*Close|.*Flush|os\.Remove(All)?|.*print(f|ln)?|os\.(Un)?Setenv). is not checked
  fix: true
